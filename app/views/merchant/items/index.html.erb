<h1>My Items</h1>

<div class="my-items">
    <% @merchant.items.each do |item| %>
      <ul>
        <li><%= link_to item.name, "/merchant/#{@merchant.id}/items/#{item.id}" %></li>
      </ul>
    <% end %>
</div>